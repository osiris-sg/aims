// ---------------------------------------------------------------------------
// AIMS Guide assistant — built-in knowledge of the CURRENT app, compiled from
// a full codebase survey (2026-07-27). This is the assistant's ONLY source of
// app truth beyond the per-request screen list, so keep it accurate and terse:
// it rides on every /guide/stream request (behind a prompt-cache breakpoint).
// When a feature ships or changes, update the matching section here (and add
// a data-tour anchor + prebuilt guide when it deserves a walkthrough).
// Retired modules (Documents, standalone Invoices, Payments, Assets, User
// Management, Audit, Analytics, Integrations) are deliberately absent.
// ---------------------------------------------------------------------------

export const APP_KNOWLEDGE = `
=== CORE CONCEPTS ===
- Sales documents (quotation, sales order, delivery order/DO, invoice, debit/credit note, purchase order/PO, purchase return, stock adjustment) share ONE editor. Create buttons on Sales list pages open it. There is NO autosave — press Save (top right) to keep a draft. Confirm finalises a document, locks it, and assigns its number.
- Paper trail: Quotation → (confirm) → Delivery Order → Invoice. Line items carry across via the editor's "Extract from…" menu actions. Confirming an invoice deducts stock and posts to the ledger.
- CREATE vs UPLOAD: every Sales list page has "Create <type>" (blank editor; a numbering-format picker and template picker may appear first when several are active) AND an "Upload <type>" button (upload existing PDFs/images or a ZIP → AI-extracts each into a draft for review).
- Editing a CONFIRMED document: it can't be edited directly — open it and use ⋮ → "Create Revision", which makes an editable new revision.
- Deleting documents happens from the LIST pages (trash icon, shown on draft/unconfirmed rows only), not inside the editor (only Official Receipts have in-editor delete).
- Every list page shares the same shell: search box, "Filter" button (right-side drawer), sortable column headers, page-size dropdown, pager, a primary Create button and sometimes extra action buttons beside it.
- Notification bell (portal header, top-right, sitting just below the "Viewing as org" dropdown; on mobile it sits in the top bar): a persistent badge with the unread count. It notifies OFFICE users (anyone with document access) when a delivery completes: one notification when its Delivery Order is ready and one when its draft invoice is created. When a RETURN completes it notifies that the Return Delivery Order (RDO) is ready. Clicking a notification opens that document. "Mark all read" clears the badge. Riders/field-tech do not receive these. Notifications wait in the bell until read, so a completed delivery or return is not missed. A draft DO or RDO created at schedule time does NOT notify (only on completion).

=== DOCUMENT EDITOR (opens from any Sales list) ===
A small "Hide fields / Show fields" toggle sits at the right end of the General/Details tab row — collapses the header form to give the items table the full screen.
Toolbar (left→right): Back; Prev/Next document arrows; Undo/Redo arrows (revert in-editor edits made since the page was opened, e.g. a deleted line item — history is per-session, not saved); "Ask AI" (AI assistant drawer: chat, attach PDF/image to extract, Apply-suggestion cards that fill the form); Preview/Edit toggle (hidden once confirmed); type-specific Confirm button; Send Email; Save; ⋮ more-actions menu.
- Confirm variants: "Confirm Delivery Order" (needs a linked Project first when enableQuotationProjectLink is on); "Confirm Invoice" split-button (dropdown adds "Confirm & make recurring" → jumps to recurring-invoice setup prefilled); "Confirm Quotation" (only with enableConfirmQuotation; auto-creates an Order and offers "Go to Order"); POs use "Receive" mode first (enter Received Qty per line, then Confirm Purchase Order); purchase returns and stock adjustments have their own Confirm dialogs.
- SEND EMAIL: invoices and quotations only, available in every status including confirmed/awaiting-payment. Saves first, then opens the email dialog: TO/CC/BCC recipient chips (TO prefills from the document's Attention contact when name+email+phone are all filled), editable subject and body, the document's PDF attached automatically. There is NO WhatsApp send in the editor (WhatsApp lives under CRM).
- "Mark as Paid": invoices awaiting payment — opens the record-payment dialog.
- "Sync to Xero": invoices only, only when opened from Accounting and enableXeroDocSync is on — creates/updates a DRAFT invoice in Xero.
- ⋮ menu: New <type>; Duplicate; Create Revision (confirmed docs only); Extract from Quotation (on DO / sales order) and Extract from DO (on invoice); Print/PDF (switches to Preview then browser print — this is how you save a PDF); Locate (jump to another document); Stock Card lookup; History & notes (audit timeline + add notes); Review Posting (invoice, AI-suggests GL account per line, dry-run); View Original (docs created from an uploaded file); DO-only: Show Route, Share delivery link (a run-scoped guest link for an EXTERNAL driver: with the tokenised URL and no login they see the run's items, deliver each with condition photos, and capture ONE customer signature that finalizes the run, commits the DO and fires the draft invoice; no skip/add/edit/cancel. The link expires around the scheduled time, can be revoked from the same menu, and auto-revokes once the delivery completes), Revoke delivery link (kills the current guest link), Bulk complete (mark all lines delivered + trigger invoice).
- Header fields: customer/supplier code box with search icon (opens the picker dialog); "+ Add contact" creates a customer contact inline; the Attention trio (person/phone/email) is what prefills email TO; Project picker on quotations/DOs (with enableQuotationProjectLink, after a customer is chosen; "Create new project" inline).
- Line items: Add Item (product picker); Add Service (with enableServiceItems); pencil icon re-picks a row; drag handle reorders; trash deletes a line; discount %/$ toggle; price-tier tag icon (asset custom prices); clock icon shows price history (pick one to apply). Footer sub-tab holds Notes and Terms & Conditions.
- Unsaved changes prompt on leaving: Keep editing / Discard / Save & leave. A concurrent-edit banner shows if someone else has the doc open (Take over / Reload).
- Official Receipt (OR) editor extras: Offset Transactions grid allocates the receipt against open invoices (must balance to 0.00 before saving); side buttons: Debtor Statement, Manual Offset (apply credit notes against invoices — the only place credit notes are applied), Audit Trail.

=== ACCOUNTING ===
Finance Hub (/portal/accounting): KPI tiles (click to drill into the matching report), Action Queue, Quick Actions (New Journal Entry, Close Period wizard, New Invoice, links to Recurring/Fixed Assets/Budgets/Bills/Posting Queue/Bank Rec/Xero/Reports/Settings), and the "Ask AI" drawer (streams answers about YOUR numbers; can attach a PDF/image, e.g. a supplier statement to cross-check).
Purchase Journal (/portal/accounting/payables/purchase-journal, old /portal/accounting/bills path redirects): supplier bills. Status tabs All/Unconfirmed/Pending Approval/Awaiting Payment/Paid/Void. "New Purchase Entry" asks which kind: Supplier Invoice (SIN, normal bill) or Supplier Purchase Return (SPR, supplier credit/debit note — posts reversed Dr Trade Payables / Cr expense, reduces payables, no payment action; SPR chip on the row). "Upload Supplier Invoices" = bulk intake (many files or ZIP → AI-extracted word-for-word, reviewed one-by-one with prev/next, each saved as its own bill — always SIN). Row actions: submit for approval (draft), approve & post (green check), reject (red X), record payment (card icon, posted bills with balance), view/edit (editable only while draft/pending), void (creates reversing journal if posted). Bill editor: dropzone to auto-fill from a file, tax treatment (exclusive/inclusive/no tax + tax code), line accounts (blank = auto), "Review" = AI-suggested GL accounts dry-run, attach source documents, "Sync to Xero" (flag-gated). Payments record amount/date/method/bank account + proof attachment; partial payments allowed.
Accounts Receivable (/portal/accounting/receivables): customer balances workspace (cut-off date, search, click a customer to drill into their running-balance ledger, Statement of Accounts button) + buttons to record an Official Receipt or post a Journal Voucher + "View Reports" (receipt listing, ageing analyses, debtor listing…). Invoice list tab has per-row "Record payment". Recording customer payments/receipts = Official Receipt (allocate against invoices) or the invoice row's Record payment.
Accounts Payable (/portal/accounting/payables): the Bills screen embedded + aged payables reports, supplier statement (pick supplier + dates + Run), purchases by supplier.
Posting Queue (/portal/accounting/posting-queue, ADMINS ONLY): ingested/unconfirmed documents & draft journal vouchers grouped by batch. Select rows → "Post to GL" or "Reject" (with reason). "Review" per row shows AI-suggested Dr/Cr accounts before posting.
General Ledger (/portal/accounting/ledger + /general-ledger): all-accounts browser (period presets, search, click an account code for its detailed ledger, Excel export, Print). Audit Trail: filter/search journals, view lines, Post a draft, Void (reversing entry).
Manual journal entries: Finance Hub → "New Journal Entry" (or AR → "Journal Voucher"): date, type, reference, Dr/Cr lines with account autocomplete; Save only when balanced; posts as UNCONFIRMED and waits in the Posting Queue.
Close Period: Finance Hub → "Close Period" wizard — month-end (locks period) or year-end (retained-earnings rollover + lock); runs preflight checks first; unlocking is admin-only.
Bank Reconciliation (/portal/accounting/bank-reconciliation): pick bank account, upload a PDF statement (AI-extracted) or import CSV (column mapping dialog), auto-match, then per line: "Post as new" (pick contra account, AI "Suggest" available), ignore, or unmatch. Summary tiles show bank vs GL balance.
Recurring journals (/portal/accounting/recurring): templates with frequency/next run + Dr/Cr lines; Active switch; play icon runs now (posts unconfirmed → Posting Queue).
Recurring invoices (/portal/accounting/recurring-invoices): schedules with customer, template, frequency, line items supporting date tokens like {MONTH YEAR}; switch between "Draft for review" and "Fully automatic (confirm + email)"; play icon generates now; can be created prefilled from an invoice's "Confirm & make recurring".
Reports (/portal/accounting/reports): directory of report cards (searchable, star favourites) — GL Detail/Summary, Account Transactions, Trial Balance, Journal, Profit & Loss, Balance Sheet, GST (Details + F5 Summary tabs), Expense Listing, Bank Summary, Aged Receivables/Payables Summary+Detail, Cash Flow, Budget vs Actual, Fixed Assets register, Statement of Accounts (per-customer; Download CSV / Print / Print-all ZIP — NO email/send option exists), Sales by Customer, Purchases by Supplier. Every report: set filters then "Update"; Print and Export CSV in the footer. Several legacy reports (debtor statement, audit trail tab, receipt listing, ageing, JV listing) only appear with enableLegacyAccountingUx on.
Budgets (/portal/accounting/budget): spreadsheet grid per P&L account × month, "Copy from last year", "Save all".
Fixed assets (/portal/accounting/fixed-assets): register with add/edit dialogs (cost, salvage, method); depreciation auto-posts during month-end close.
Xero (/portal/accounting/integrations/xero): Connect (OAuth), Sync now (pull chart+contacts), AI Auto-map, per-row account mapping. Flags: enableXeroIntegration, enableXeroDocSync.
Accounting Setup (/portal/settings/accounting-setup) tabs: Default Settings (base currency, last-price switches, perpetual inventory, opening balances, DOCUMENT NUMBER SEQUENCES — add variants per doc type with a visual block builder, reset policy, start number, apply-to-all switch); Financial Settings (currency + standing FX rates, financial year end, GST config, tax defaults, period lock date, timezone); Tax Rates (add/edit/deactivate GST codes); Accounts Definition (chart of accounts: code ranges, control accounts, add/edit/deactivate accounts, seed defaults when empty); Inventory Cost (per-unit cost grid); Cost Centers; Account Mapping (map products to sales/rental GL accounts; add services with GL account).

=== SALES & INVENTORY LISTS ===
- With enableDocumentListView ON, each Sales/Inventory document page shows a list (stat cards: Total / This Month / Unconfirmed; Create + Upload buttons; eye = open in editor; trash = delete, drafts only; filters: created-on, status, customer). With the flag OFF, the page auto-opens the LATEST document of that type in the editor (and auto-creates one if none exist).
- Upload <type> dialog: drag-drop multiple images/PDFs (10MB each) or a ZIP (25MB); "Upload & Extract" AI-creates unconfirmed drafts (matches PO + customer, codes lines from the Revenue Master File); per-file ✓/✗ results with Open buttons.
- Inventory purchases/purchases-return/adjustment-in/adjustment-out work exactly like Sales lists for PO/PR/SAI/SAO. Supplier bills moved to /portal/accounting/bills.
- Stock Card (Sales → Stock Card or Inventory → Stock Card): search a product's stock position; eye icon opens the product detail dialog.
- Inventory list (/portal/inventory, also Master Files → Inventory): "Add Items" drawer (pick product, quantity → auto-generates SKUs), QR icon per unit, eye opens the unit page (timeline history, parent/component units), trash deletes; inline SKU rename needs enableEditInventorySku. Filters: status (rental/reserved/maintenance/sold/instock), category, product.
- Products (/portal/inventory/products, also Master Files → Products): table or hierarchy view; Add Product; pencil edits; delete requires typing the product name; hierarchy view adds parts under parents. Every product carries a Type of Equipment or Accessory (set on the create/edit form, next to Unit of Measure; defaults to Equipment, and every pre-existing product is Equipment). Type decides how many photos the field must capture when a unit is tagged: Equipment needs 4, Accessory needs 1.
- Inventory → Reports is a "coming soon" placeholder — send users to Accounting reports or Stock Card instead.

=== MASTER FILES (Organization Settings → Master Files) ===
Tabs: Customers / Suppliers / Products / Inventory / Services.
- Customers (/portal/customers): Add Customer drawer (name, email, phone, address, GST reg no, currency, salesman, multiple contacts); pencil edits; eye opens the customer page (add/edit/delete Site Offices there); trash deletes.
- Suppliers (/portal/suppliers): Add Supplier drawer (name, email, phone, address, GST reg no, currency); edit/delete; no detail page.
- Customer Information (/portal/customer-information): a bottom-of-nav list (one row per collection request) for gathering a customer's contact people per project WITHOUT the office typing them in. Each row shows Customer, Project, Status (Awaiting response / Submitted / Expired / Revoked), Submitted date and Created date; the eye opens the request to see the collected contacts, split into "Delivery Order (DO) contacts" and "Invoice contacts" (name, email, phone each). "Add Customer Info" (top right) picks an existing Customer then Project (no inline create) and generates a unique shareable link; the office sends that link to the customer. The link opens a PUBLIC no-login page showing the customer + project read-only with the two contact sections; the customer adds people to each and submits. The link works for 30 days, is revocable (Revoke on the row or detail) and regenerable (a fresh link that revokes the old one). Resubmission is allowed: reopening the link prefills the previous answers so the customer can correct them, and every submission is kept (superseded, not overwritten). Submissions are stored STANDALONE (their own table, customer/project held as plain ids) and are NOT yet linked to the customer's saved CustomerContact/ProjectContact records. Gated by the customer-info:read and customer-info:create permissions (superadmin/Admin).
- Services: Add Service (code optional, name, unit price, GL account required); edit/delete; these become pickable "Add Service" lines in the editor.

=== PROJECTS ===
List (/portal/projects): Add Project → 3-step wizard (details + inline add-customer/site-office → optional add items from inventory → review/submit); eye opens the project, pencil edits, trash deletes; filters: status, customer, start/end dates.
Project page tabs: Active on Site / Past Deployments / Sales & Services / All Invoices / Quotations / Field Reports.
- "New Deployment" dialog: type Rental (recurring, Monthly Rate) / Sale / Service, deployed date, notes.
- Off-hire a deployment: stop-circle icon on an active deployment card (also auto-pauses its linked recurring invoice schedules).
- "Add DO" on a deployment card attaches an unattached delivery order; "View details" expands deployed items, DOs and monthly invoices.
- "Create Quotation" on the Quotations tab makes a quotation pre-linked to the project.
- Field Reports tab shows service reports + delivery scans from the field app (photos, routes).

=== ORDERS ===
/portal/orders. Slim list (default): pipeline-stage tabs (QO Confirmed → DO draft/confirmed/delivered/installed → Invoice draft/confirmed), search, eye opens the order. Order page: tick line items → "Create DO from selected" / "Create Invoice from selected"; open the source quotation or any linked document chip. With enableCappitechOrders ON: also Create SO/PO from selected, per-line discounts with recalibrate, and a "Upload & Verify" panel that scans supplier DOs / tax invoices against the order (batch verification with per-file status).

=== DELIVERIES (delivery runs) ===
/portal/deliveries: queue of physical delivery runs recorded by riders in the field app (no DO needed up front). Shows ALL runs, newest first; the "Linked DO" column tells you which are linked, partly linked ("k of n linked"), or unlinked ("—"). With enableUnifiedRuns ON, DO-first deliveries ALSO appear here as runs whose items are born linked to their DO (same field proof). When a standalone run is COMPLETED it now auto-creates a real Delivery Order from its items AND a DRAFT (unconfirmed) invoice — the invoice AUTO-PRICES its own lines from the asset (rate x quantity, no duration: Asset.price for sale lines, the "Monthly Rental" custom price for rental lines, per each unit's rental/sale intent; 0 when the asset has no matching price), stays a DRAFT, and the office reviews/adjusts + confirms it here. Delivery Orders are a GOODS document — Item / Description / Quantity only, no prices; pricing lives on the invoice. You can still manually link a run's items to an existing DO, or create one from a subset. Click a row to open the run. Each row also has a ⋮ actions menu: Cancel appears for a scheduled run, or an in-progress run that has delivered and linked nothing (it marks the run cancelled and returns any reserved units to stock); Delete appears for draft or scheduled runs only, which have committed nothing, and removes the run plus its unconfirmed draft DO. A run that has delivered or linked anything, or is completed, shows neither action (cancel where allowed, otherwise unwind through returns; a committed run is never hard-deleted). "Schedule a delivery" (top-right) pre-creates a run for a future date. Field order: CUSTOMER → PROJECT → DELIVERY ADDRESS → products → date + time (both settable). Project is REQUIRED (the rider is matched back to this run by the project they assign in the field). The DELIVERY ADDRESS auto-fills from the project's address (editable) and lands on the DO's "Deliver To". Products are either catalog items (asset + quantity) OR "Free type item" description-only lines (no unit ever binds to them; they carry to the DO as plain lines the office resolves later). A free-typed line has no catalog product to read its Type from, so the row carries its own Equipment/Accessory picker, defaulting to Equipment. "Extract from quotation" (after a customer is chosen) autofills project/address/PO and best-effort maps quotation lines to catalog products (unmatched lines become free-typed). The draft DO now renders the PROJECT name + Deliver To, and a qty-N product is written as N per-unit lines so each unit stamps its own serial as it binds ("Rental of N units of {name} / Model / S/No." block) and the invoice prices per unit. It lands as a scheduled run with no rider and NOTHING reserved (a "Scheduled" column shows the date), AND immediately creates a DRAFT Delivery Order carrying that PO number ("Your PO No.") — a GOODS document (no prices; the invoice prices itself at completion). A rider fulfils the run WITHOUT any special "claim scheduled" step: they start any matching unit as a normal ad-hoc delivery and, at the assign step, pick THIS scheduled delivery from a list of the office's open runs shown by drop address (they no longer pick a customer then project; the chosen run supplies both). The backend then MOVES that unit (its live item, its condition-photo/GPS proof, and its reservation) into the chosen scheduled run, binds it into the DO's matching asset line, and drops the throwaway ad-hoc run. Every open run is listed, so a run whose office-entered asset does not match the unit is still pickable (it flags the mismatch and adds the unit to that run as an extra line) rather than hiding it and stranding the rider over a typo. The rider lands in the scheduled run's basket, where a per-asset "N scheduled · N delivered · N remaining" summary and a "Scan another <SKU>" button let them load the remaining units straight into the open slots. At completion the pre-created DO is committed (stock deducted, delivered_installed) and its invoice fired — no second DO is created. Cancelling a scheduled run deletes its draft DO if still unconfirmed (leaves it if the office already confirmed). Riders see a "Scheduled deliveries (N)" badge on the /scan home (informational only — the manifest of what's due); the scheduled-run basket shows the PO number, customer/site and line summary plus a "View full DO" link. (Unscheduled runs still auto-create their DO + draft invoice at completion, as before.)
Run detail (/portal/deliveries/[id]): items table with per-item checkboxes (unlinked items pre-ticked) → "Link selected to existing DO" (picker, defaults to the run's project's DOs) or "Create DO from selected" (creates a DRAFT/unconfirmed DO pre-filled from those items, then opens it in the editor to add pricing and Confirm). Items on one run can go to DIFFERENT DOs — link a subset, repeat for the rest; each item row shows its linked DO chip. Stock deduction + DO status stamping happen when the DO is CONFIRMED (linking to an already-confirmed DO applies them immediately); delivered units stay reserved until then. Each item row also has a per-unit Rental/Sale toggle: it sets that unit's deployment commercial type (default Rental) — Sale is disabled until the unit is assigned to a project (nothing to write to). It does NOT flip the unit's stock status itself; the reserved→rental/sold flip still happens when the DO is confirmed/acknowledged, which reads this type. Linking does NOT create an invoice (invoicing stays manual). Below the items: proof of delivery from the field (photos, customer signature, GPS per step). A run cannot be cancelled once any item is delivered or linked to a DO. Field tag-binding: when a tech binds a tag to a new unit whose asset has child assets (e.g. SIDS → TSS, Sim Card), the bind screen shows one section PER CHILD ASSET (labelled by its real name — TSS, Sim Card) below the unit's serial: type the child's serial + tap its own tag. All optional (bind the parent alone, or tag any children). "Add a child asset type" (name + code only; office completes the rest) creates a new child asset under the parent, ready to tag on the spot. Children are tagged only at parent-bind time; already-tagged parents get children from the office dashboard. If the scanned nameplate matches no catalog product at all, the tech can "Create new product" (name prefilled from the OCR'd model + an auto-suggested code, plus an Equipment/Accessory Type toggle that defaults to Equipment) instead of being blocked — it's a NEW top-level product that lands in the auto-created "New" category for the office to price and re-file. A dedupe guard runs first: a normalised-exact name/code match auto-selects the existing product (and near matches show in the picker as "did you mean") so OCR misreads don't mint duplicate catalog entries.
Field-side flow per unit (signature LAST): mandatory condition photos at start (how many depends on the product's Type: EQUIPMENT needs 4 and gets a GUIDED sequence prompting front, then back, then overall appearance, then one more angle, with a slot strip showing what is captured and what is next; ACCESSORY needs 1 and keeps the free-form picker. The rule is enforced in the field app AND on the server, so a short set is rejected either way) → acknowledge (GPS + photos, saved unsigned) → assign to a scheduled delivery in-flow (creates the deployment): instead of picking a customer then a project, the rider picks one of the office's open scheduled deliveries from a list shown by drop address, and the chosen run supplies the project + customer and merges this unit into that run. Every open run is listed; one with no matching item for this unit's asset is flagged but still pickable (so an office typo cannot strand the rider), and picking it adds the unit to that run as an extra line. If there are NO open scheduled deliveries the rider is told the office must schedule one first (there is no ad-hoc or create-new fallback) → "Installation needed?" prompt (install photos captured there if yes) → the item is MARKED DELIVERED (no signature at this point). The ONE customer signature is captured at the END of the whole run via the "Get customer signature" step (reached once every item is delivered; a skipped item holds the run open until the rider comes back to it); that single signature completes the run, creates the DO + draft invoice, and covers delivery + installation for every item at once. Signing no longer happens per item. Completing the run auto-creates its Delivery Order + a draft invoice; after finishing, the rider lands on a simple "Delivery completed" screen with a "Print DO" button (prints the delivery receipt at hand-off) and a "Back to scan" button. That screen no longer shows a summary of what was created. The receipt also stays reprintable later from the finished list ("Reprint a delivery" on scan home, last 7 days), and the OFFICE is told the DO and draft invoice are ready via the portal header notification bell (see the bell entry above). Leaving mid-flow is resumable — the item's basket button becomes Continue and re-enters at the right step. WALK-THROUGH (SCHEDULED runs only): instead of picking items off a list, the rider is stepped through every line the office scheduled, one at a time, in the order the office entered them ("Item 3 of 7" with the product name). A quantity of N is expanded into N separate positions, so each unit is walked and stamped individually. At each position the rider scans a tag, keys a serial, taps Start Delivery on a free-typed line, or taps "Skip this item" to pass it over and move on. A FREE-TYPED line (a cable etc., no unit to scan) now runs the SAME full lifecycle a unit does on its own screen: Start Delivery with guided condition photos (5 for equipment, 1 for accessory, from the line's own stored Type), then End Delivery with a customer signature. It no longer just gets a one-tap "Mark delivered". Its proof is written as normal DO_START / DO_ACK reports with no asset and no unit, tied to the run, so it shows in the office proof panel and the DO's proof section like any other line; it is only absent from asset/project service-history (a cable has no asset history). Any matching unit is accepted at any position, not just the one on screen. After the last position the rider lands on the basket. "View all items" leaves the walk early and "Resume walk-through (N left)" goes back into it. AD-HOC runs are unchanged and keep the free-order basket. The basket then splits into DELIVERING (everything in flight, covered by the bulk "End all delivery" / "End all return" button) and SKIPPED (the passed-over items, each with its own Start Delivery / Start Return / Load a unit action; a free-typed line's Start Delivery re-enters its full photos-then-signature flow). The bulk button NEVER touches skipped items: they are picked up individually or left behind, which is the point of skipping them. OUTBOUND delivery moved the customer signature to the END of the run (2026-08). "End Delivery" (per item) and "End all delivery (N)" (bulk) now just MARK items delivered — condition photos were already taken as each unit was delivered, and NO signature is captured here. There is NO acknowledgement photo step - condition photos are captured ONLY at start (the guided 5-angle set); marking delivered records just a signature-less proof. Ending the LAST unresolved item flows STRAIGHT into the ONE customer signature; a "Get customer signature" card is also shown whenever the run is fully delivered as a fallback (a resumed run that was already fully delivered, or a rider who backed out of the signature). That single signature completes the run and creates the Delivery Order + draft invoice, so the DO and invoice can no longer generate while items are still outstanding. A SKIPPED item does NOT resolve the run - a skip means "not now, come back to it", so the run stays open (in_progress) with no signature, no completion, no DO commit and no invoice until every item is actually delivered. Clicking an in-progress run resumes the walk-through over the items still to deliver, the skipped ones included, rather than dropping the rider on the basket. RETURNS now mirror outbound: the signature is captured ONCE at the END, not per collection. "End Return" (per unit) and "End all return (N)" (bulk) COLLECT items with NO signature (each collect flips the unit rental->instock and off-hires its deployment); a skipped item holds the run open. Once every unit is collected the run flows STRAIGHT into the ONE customer signature (no install step for a return); a "Get customer signature" card is also shown as a fallback if the rider backs out. That single signature completes the return and creates the RDO. Once a unit's delivery is finished, re-scanning it no longer shows a "Completed" card or the items list — the scan chooser drops the finished delivery and shows only what's still actionable (e.g. Start Return for a now-rental unit). REVERSE DELIVERY (returns): scanning a unit that's currently OUT on rental shows a "Start Return" action — the same field flow (condition photo + GPS at pickup → signature to complete) but with NO assign and NO install step. In the basket each returning unit has its own "End Return" button (the twin of End Delivery) that COLLECTS just that unit with no signature; "End all return (N)" collects them all at once. The one signature comes at the end. The condition-photo minimum is the SAME on a return as on the way out (equipment guided sequence, accessory 1); on a return the guided capture goes ONE ANGLE AT A TIME and, after each shot, shows it BESIDE the matching outbound angle so the rider compares before and after per angle (outbound photos are angle-labelled at capture; units photographed before that shipped fall back to pairing by position plus the full outbound set with a "not a guaranteed angle match" caption). Collecting a unit flips it rental → instock and auto-off-hires its deployment (stops recurring rent at the return date) — but only when it's the LAST unit still out on that deployment, so a partial return never stops billing for units still on site. Completing a return run auto-creates a goods-only Return Delivery Order (RDO) — no prices, no invoice, no GL. A SOLD unit can't be returned this way (it's blocked with a message to raise a Credit Note instead). DESCRIPTION-ONLY (free-typed) lines are returnable too: a cable etc. that got its own deployment when delivered now shows in the "Schedule a return" picker (by its description, no serial). On the walk it takes condition photos exactly like the outbound free-typed flow (guided per angle for equipment, one for accessory, from the line's stored Type), on its own page with no unit to scan; there is no per-line signature on a return, so once its photos are captured it is collected and its deployment off-hired. Collecting the last unit flows into the one end-of-run signature (there is no separate return-photo screen and no per-unit signature). The basket's "Free type item" dialog (for something not in the catalog) also asks for an Equipment/Accessory Type, defaulting to Equipment, since there is no product to read it from.

=== CRM ===
/portal/crm — WhatsApp: Connect WhatsApp (Meta signup popup) or connect an existing WhatsApp Business app number; once connected: send a template message or free-text (24h reply window), Recent messages log, Disconnect. AI Agent tab: enable the agent (drafts a reply for every incoming message), optional auto-send with an allowed-topics box, general instructions, training example pairs (required before auto-send), and a test panel. Suggestions tab: review queue — edit the suggested reply, "Approve & send", or Dismiss; history below. Scheduled tab (/portal/crm/scheduled): "Schedule message" button opens a dialog (recipient with country code, message, send-at date/time); a background scheduler delivers due messages every minute; the dialog has a Repeat option (Once / Daily / Weekly / Monthly / Custom every-N-days) with an optional "repeat until" end date — recurring rows re-arm to their next occurrence after each send and stay PENDING until cancelled or the end date passes; the table shows a Repeats column (×N sent) and a Cancel action on pending rows; free-text scheduled sends only reach recipients who messaged the number within 24h of send time. Recipient fields across CRM (send panel, Scheduled dialog, agent test) are searchable dropdowns of past WhatsApp contacts showing the best-known name (phone address book > WhatsApp profile name > matched AIMS customer); typing a fresh number still works. Contacts tab (/portal/crm/contacts): lists every number messaged with, best-known name, last-message time, and a per-number AI auto-reply control — Approve (AI may auto-reply), Default (follows the global agent setting), or Block (AI never auto-replies to this number, though it still drafts a suggestion for manual review).

=== SERVICE REPORTS ===
/portal/maintenance-reports: list of field service reports (search, click a row to open). Reports are CREATED IN THE AIMS FIELD APP (NFC scan flow), not in the portal — the New Report button is intentionally disabled. Report page: full checklist, remarks, technician + client signatures (signed in the field app); "Create Invoice" (only when payment is required and none exists yet) / "View Invoice"; Print (browser PDF).

=== OTHER REPORTS ===
/portal/reports: Statement of Account (pick customer + dates → Generate; Export CSV; Print; aging buckets — NO email-to-customer action exists) and Price History (filter by customer/item/dates; Export CSV).

=== SETTINGS & ADMIN ===
Company Profile (/portal/settings/company-profile) tabs: General (name, address, phone, reg no, currency), Bank Details (shown on documents), Branding (logo + default stamp), Document Names (rename document types per org), Doc Defaults (default T&Cs / notes / footer per document type — new documents inherit these).
Admin Panel (/portal/admin, admins only): configuration (modules, custom fields, UI theme, feature-flag switches), organizations management (orgs, API keys, email ingestion, templates). Every sidebar module — including Deliveries — is a per-org toggle on the Modules tab (Admin Panel → Configuration → Modules, or the org's Modules tab under Organizations); switching one off hides it from the sidebar for that org.
Uploads log (/portal/admin/uploads, admins only): the record of documents submitted from the field /submit app (async intake) — each file's status (Queued/Processing/Done/Failed), the failure reason, a link to the original file, and a Retry button for failed extractions. This is where /submit failures surface (a failed extraction creates no draft, so it never reaches the posting queue).
Account/password/profile: the user avatar menu at the bottom of the sidebar (Clerk account panel) — not a portal route.
Each document's own change history: editor ⋮ → History & notes.
Activity Log (/portal/audit, "Activity Log" tab — ON by default for every org via the enableActionLog flag; needs the audit:read permission): the full user-action log — every API action with time, actor (users, API keys, guests via share links, and automated jobs shown as "System creation"), action chip, resource, request path, success/failure + duration + IP. Filters: actor type, status, date range, search. An admin can switch the flag off per org, which reverts the page to the legacy Audit Logs table only.
`;
