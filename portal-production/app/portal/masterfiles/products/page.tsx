"use client";

// Master Files → Products tab (re-uses the existing inventory/products page).
import ProductsPage from "@/app/portal/inventory/products/page";

export default function MasterFilesProducts() {
  return <ProductsPage />;
}
