"use client";

// Master Files → Inventory tab (re-uses the existing Inventory page).
import InventoryPage from "@/app/portal/inventory/page";

export default function MasterFilesInventory() {
  return <InventoryPage />;
}
