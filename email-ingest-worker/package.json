{
  "name": "aims-email-ingest-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Email Worker: inbound email → AIMS ingestion webhook (docs+{orgId}@domain → AR/AP drafts)",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "postal-mime": "^2.4.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
