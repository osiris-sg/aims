# AIMS — Project Handoff / Context Doc
_Last updated: 2026-07-08. Maintainer: Elroy. This doc orients a fresh Claude/Claude Code session._

## 1. Repo + Deploy Topology
- Monorepo at `~/Desktop/aims`. **Branch topology (migrated 2026-07-08): `main` = production** (was `yx/dev`, renamed on GitHub; repo default flipped master→main). **`elroy/dev` = Elroy's staging/work branch** — Vercel previews of it serve at `aims-mocha.vercel.app` (domain branch-pinned). Teammate **YX** pushes to `main` directly.
- `api-server-production/` — NestJS + Prisma + Neon Postgres + Clerk auth + Xero. Deploys to **RENDER** from `main` (verify the dashboard branch setting was flipped post-migration). Build runs `nest build && npx prisma db push --accept-data-loss` (schema pushes on deploy). `prebuild: npx prisma generate`. A SECOND staging Render service runs against the legacy DB (see §3 DBs).
- `portal-production/` — Next.js 14.2. Deploys to **VERCEL** from `main` (production branch set via API 2026-07-08). Vercel requires commit author email `2300950@sit.singaporetech.edu.sg`.
- Separate repo: `~/Desktop/water-sg` (T3/Next.js monorepo, app at `apps/water-sg/`), branch **main**, Vercel, prod domain `app.biofuelindustries.sg`.
- Frontend→backend URL env var: `NEXT_PUBLIC_BACKEND_API_URL` (Vercel). NOT `NEXT_PUBLIC_API_URL` (doesn't exist — a bug we fixed).

## 2. Working Rules (follow these)
- **Investigate read-only first** → report → build → **AUDIT diffs → STOP before commit** → explicit approval → commit.
- **Stage explicit file paths only.** NEVER `git add -A`. Persistent noise stays unstaged: `.DS_Store`, `.claude/settings.local.json`, both `package-lock.json`, `android/gradlew`, `public/company-stamp.jpg`, `eugene-signature.png`.
- **Sync discipline:** `git fetch origin && git rev-list --left-right --count origin/main...main` (or `...elroy/dev` when working there) before push (want `0 0` after). YX races happen on `main`: if push rejects, rebase the single commit on top (usually zero file overlap), re-push.
- **DB scripts:** Neon WebSocket adapter one-liner pattern (see §7). Always DRY RUN printing a plan, then `APPLY=1`. Target rows by **exact ID**, not name (name collisions exist — e.g. an INVOICE named "DO202607-002").
- **Prod testing:** user tests on deployed prod (Render+Vercel). Deploy BEFORE testing. Local backend runs need `npx prisma generate` after YX schema pulls.

## 3. Org / Entity IDs
- **Biofuel** org: `52e90ba8-bfbd-48b0-bb76-4f9667bf74f1`
- **Cappitech** org: `59802f75-262b-4f96-b8b2-09a9a071d882`
- KEEP docs (survived bulk cleanup): DO `7babe78c…` (DO202607-002, the ZZTEST test DO) + QO `7b3c3c28…` (QO1202606-003). All other DOs/QOs were deleted.
- ZZTEST scaffolding — **mostly gone after the 2026-07-08 wipe**: project `ec755197…`, its deployments/assignments, and untagged unit ZZTEST-AST-003 `786516c1…` DELETED. Still existing: customer `c5cbfd2e…`, asset `976f1f1c…` (skuKey ZZTEST-…), tagged units ZZTEST-AST-001 `df0d0497…` / -002 `a15ca654…` (reset to instock, tags still bound: 001 → `99:72:cf:01:00:00:02`, 002 → `99:78:63:01:00:00:02`), and the 2 KEEP docs above (project-unlinked).
- **Email-ingestion E2E test artifacts — CLEANED UP 2026-07-06.** Deleted (dry-run-verified zero dependents, then applied): 3 draft Documents (INVOICE `BIPL-JPSG-INV-20260630-0077` `6a898732-…`, BILL `JP2606030067` `fbe08bcf-…`, BILL `JP2606230079` `42c19916-…`) + all 6 S3 objects under `email-ingest/52e90ba8-…/` (both test runs). **Kept deliberately:** EmailIngestLog rows (`test-ingest-001/002/003` + the real-Gmail run — audit history); EmailIngestConfig for Biofuel (`8ea96866-…`, live config: enabled, watchedSenders=[elroylee295@gmail.com]); Supplier `647eb442-…` Jurong Port Pte Ltd (pre-existing real supplier, NOT a test placeholder). Nothing left to clean from these tests.
- **DBs:** PROD = Neon `ep-icy-moon` (pooler, ap-southeast-1) = `DATABASE_URL`. STAGING = Neon `ep-gentle-bonus` = `DATABASE_STAGING_URL` — this is the **LEGACY pre-migration database** (NOT a prod copy: ~292k old documents, no Biofuel inventory), used by the staging Render service. `npm run db:refresh-staging` (needs NEON_API_KEY + NEON_PROJECT_ID) can replace it with a fresh prod branch if a real prod-copy staging is ever wanted.
- **PROJECT/INVENTORY WIPE executed 2026-07-08** (script preserved at `api-server-production/scripts/wipe-projects-inventory.js`): Biofuel-scoped, both DBs — all 220 projects + 313 deployments + 1,692 assignments + 594 untagged inventory units deleted; all 52 tagged units reset to instock/location-null/quantity-1 for field re-assignment. Docs survived with project links auto-nulled (stale `config.projectId` may linger inside some config JSONs — cosmetic). Tagged units get re-assigned to NEW projects via the field app.

## 4. Feature State (recently shipped, all on main)
- **DO per-item delivery** — items advance not_delivered→delivering→not_installed→completed; stock deducts per-item on Acknowledge (NOT on confirm); unit status flips instock→rental/sold (by ProjectDeployment.type) riding the same deductedAt exactly-once claim; completion gate + auto-invoice (idempotent via config.sourceDocumentId). Big-card stage derives from the SCANNED item's deliveryStatus. Field flow: Start Delivery → Acknowledge Delivery → Complete Installation.
- **Guest delivery** — DeliveryShareLink token → `/guest/delivery/<token>`; @Public token-scoped MSR create/sign endpoints in `src/public-delivery/`; guest captures full proof (signature, name, photos, GPS) via shared components (`components/delivery/`); technicianUserId sentinel = 'GUEST'. Share-link dialog gives absolute URL `https://www.ai-ms.io/guest/delivery/<token>`.
- **OSI-13 upload→extract** — DO list upload button → extraction → item auto-match to inventory by EXACT serial→single-instock-unit (SKU/desc only SUGGEST via suggestedInventoryIds, never auto-assign); deliveryDate/Address mapped; syncDocumentItems materializes rows from config.items[].inventoryItemId.
- **Extraction = Claude now** — `document-extraction.service.ts` routes ALL extraction through `_extractStructuredJson` (model `claude-sonnet-4-6`, temp 0, max_tokens 3000, native PDF via document block, regex-scoop JSON). OpenAI client + pdf-parse + pdf-to-png DELETED from extraction. `ANTHROPIC_API_KEY` on Render (no OPENAI_API_KEY needed; `openai` npm pkg stays — embeddings uses it). Supplier-PO recon path unchanged.
- **Extraction customer handling** — org-scoped matching; writes FLAT config fields on unique match (customerId/customerName/customerCode/customerAddress/customerEmail — customerCode is what the editor displays) + nested config.customer retained; transformer falls back to nested `customer?.id` (gated on id, NOT name — name is extracted text even when unmatched). Unmatched → customer-null DRAFT (no hard error; "must have a customer" lives only in send/confirm paths, correctly).
- **Normal-user role + submit flow** (commit 61f3e59) — third role `normal_user`: mobile `/submit` page (app/(submit)/), DO/QO/Invoice chooser → camera-first capture → existing 2-step extract→from-extraction → DRAFT → "Submitted!". Role isolation both directions via useRoleGate.ts + FieldOnlyGuard.tsx + (field)/layout.tsx: normal_user bounced from /portal AND /scan to /submit. Role seeded (prisma/seed.ts) with minimal perms: document-extraction:extract, documents:create-basic, users:read-roles, roles:read, permissions:read. Roles are per-org RBAC (Role table, @@unique[name, organizationId]) — NOT Clerk metadata.
- **water-sg ESS/ECM** — water-sg repo has new bearer-token endpoints POST /api/ess-site (siteId,siteName,lat,lng,batteryCount 3|4 default 3,customer?) + /api/ecm-site (siteId,name,lat,lng,managerId null; writes EcmSite directly, bypasses Clerk manager derivation). AIMS `water-sg.service.ts` dispatches by `asset.waterSgProductLine` at DO_ACK sign: SIDS→/api/site (unchanged), ESS→ess-site, ECM→ecm-site; fire-and-forget; persists waterSgSiteId on MSR. Render env: WATER_SG_ESS_API_URL + WATER_SG_ECM_API_URL set. Assets tagged: LION*→ESS (3), AF*→ECM (4) via migration. Feature flag enableWaterSgSites ON for Biofuel.
- **Field bind auto-select** — normalized exact match (strip non-alphanumerics) so AI-filled "AF 100" matches catalog "AF100"; Create & Bind enables without manual tap. No fuzzy/contains.

## 5. IMMEDIATE NEXT STEPS (paused on Clerk credentials)
1. Create test user in **Clerk dashboard** → log in once (creates the DB User row) → run the **Biofuel role-provisioning script** (drafted in prior chat: creates `normal_user` Role in Biofuel + connects the 5 permissions + assigns the user; DRY RUN first — the RBAC join shapes were guessed (userRole table vs user.roles connect), dry-run surfaces the real shape).
2. Test normal-user flow on phone: login → land /submit → Quotation → photo → "Submitted!" → admin sees draft w/ customer displaying.
3. Follow-up migration: `Document.createdByUserId` (submitter stamping — Document has NO creator field today; only editingByUserId which is an edit-lock). Additive nullable column + wire through createFromExtraction/createBasicDocument. Own commit.

## 6. OTHER OPEN THREADS
- **OSI-8 quotation send-email**: SendInvoiceEmailDialog is not type-aware (title/subject/body say "Invoice" for QOs) + a save-timing bug (backend checks persisted config.customer; QO can be sent unsaved → false "must have a customer"). Fix decided but paused: type-aware dialog + likely save-before-send + TO auto-populated from the quotation's Attention contact (verify Attention has an email — may be name-only).
- **water-sg ESS/ECM end-to-end test**: temp-tag ZZTEST asset waterSgProductLine='ESS' → reset DO 7babe78c to start → ack ZZTEST-AST-001 w/ signature → verify MSR.waterSgSiteId populated + EssSite row in water-sg → revert tag to null. (Scripts drafted in prior chat.)
- **Bulk cleanup**: inventory clear **DONE 2026-07-08** via the project/inventory wipe (§3) — keep-tagged-only + instock reset, no backfill dependency. Still open: invoice cleanup (~2451 Xero-mirror invoices; orphaned auto-invoices from deleted DOs exist). "Import invoice table" mystery **SOLVED**: the mass of old documents (~292k rows) lives in the LEGACY `ep-gentle-bonus` DB, not prod — nothing hidden in prod's schema.
- **Branch `bluetooth-printer-flow` (43e7aa2)** preserves the XP-58IIH Bluetooth (Classic SPP / ESC/POS) printing chain before the Sunmi pivot — `BLUETOOTH_PRINTER_FLOW.md` on that branch documents the files, the APK requirement, and the RFCOMM tuning for a clean restore.
- Dead deps removable someday: pdf-parse, pdf-to-png-converter (api-server). Keep openai (embeddings).
- Dependabot: 1-2 low/moderate pre-existing repo-wide alerts, unrelated.

## 7. Neon DB Script Pattern (all DB work uses this)
npx dotenv -e .env -- node -e "const{PrismaClient}=require('@prisma/client');const{PrismaNeon}=require('@prisma/adapter-neon');const{neonConfig}=require('@neondatabase/serverless');const ws=require('ws');neonConfig.webSocketConstructor=ws;const u=new URL(process.env.DATABASE_URL);u.searchParams.delete('pool_timeout');u.searchParams.delete('connect_timeout');const adapter=new PrismaNeon({connectionString:u.toString()});const p=new PrismaClient({adapter});(async()=>{ /* … */ process.exit()})().catch(e=>{console.error(e.message);process.exit(1)})"
Run from api-server-production/. DRY RUN printing plan first; APPLY=1 to execute.

## 8. Known Gotchas
- Document name collisions exist (an INVOICE named "DO202607-002") — select by TYPE + exact id.
- A DO revision ("Rev-1") once shadowed the original (getScanContext resolves newest DO) — deleted; if scans show wrong doc, check for revisions.
- Field bind: SKU uniqueness is org-wide but the match path is per-asset → "already exists, retry to match" dead-loop if a unit is filed under a different asset. Fix = move unit's assetId (keeps assignments) rather than delete.
- Attachments pasted into the web chat sometimes arrive BLANK — paste terminal output as plain text.
- clean tree check + branch check (`git branch --show-current` = elroy/dev for work, main for prod-bound pushes) before any build.
